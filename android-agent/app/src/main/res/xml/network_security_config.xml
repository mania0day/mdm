<?xml version="1.0" encoding="utf-8"?>
<!-- Permit cleartext HTTP only to the local/self-hosted MDM server used for
     development and emulator testing (10.0.2.2 is the host loopback from the
     emulator). Production deployments should serve the API over HTTPS/TLS. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
